install.packages("mvMORPH")
y
q()
n
library(mvMORPH)
install.packages("TreeSim")
tr
write.tree(tr)
tr
write.tree(tr)
write(tr)
write.tree()
write.tree(tr)
print(tr)
write.tree(tr)
print(tr)o
write.tree(tr)
write.tree(tr)
tr
hist(rlnorm(1000, meanlog=0, sdlog=1))
hist(rlnorm(1000, meanlog=0, sdlog=.5))
hist(rlnorm(1000, meanlog=0, sdlog=.5))
hist(rlnorm(1000, meanlog=0, sdlog=1))
sigma
sigma
sigma
res
res$rate
attr(res)
attr
res
summary(res)
res$par
res
x0
dataset
x0
res
x0
x0
res$theta
datasets
rTraitCont(tr, model="BM", sigma=n.sim[1, theta=n.sim[1])
rTraitCont(tr, model="BM", sigma=n.sim[1, theta=n.sim[1]
rTraitCont(tr, model="BM", sigma=n.sim[1, theta=n.sim[1]))
rTraitCont(tr, model="BM", sigma=n.sim[1], theta=n.sim[1])
length(rTraitCont(tr, model="BM", sigma=n.sim[1], theta=n.sim[1]))
datasets
one.dataset
rTraitCont(tr, model="BM", sigma=n.sim[1], theta=n.sim[1])
class(rTraitCont(tr, model="BM", sigma=n.sim[1], theta=n.sim[1]))
datasets
length(rTraitCont(tr, model="BM", sigma=n.sim[1], theta=n.sim[1]))
datasets
datasets
datasets
datasets
sigma
x0
q()
n
